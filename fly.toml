# ⚠️  DEPRECATED - This file is no longer used
# This application now uses Netlify for deployment
# See netlify.toml for current deployment configuration

# The application uses:
# - Netlify for hosting the frontend and serverless functions
# - MongoDB Atlas for the database
# - Netlify Functions (/.netlify/functions/*) for the backend API

# To deploy:
# 1. Push to your Git repository
# 2. Netlify automatically deploys via Git integration
# 3. Set MONGODB_URI environment variable in Netlify dashboard
